# Oficialia-Digital-DSA — dependencias exactas (Python 3.11+)
# Instalación: pip install -r requirements.txt

nicegui>=2.7,<3            # Interfaz web (bandeja + HITL split-screen)
pydantic>=2.7,<3           # Validación y tipado estricto (MetadatosOficio)
pydantic-settings>=2.4,<3  # Configuración central (.env)
pymupdf>=1.24              # Motor PDF: hash, sanitización y render (fitz)
watchdog>=4.0              # Vigilancia de storage/01_entrada/
google-genai>=1.0,<2       # SDK oficial de Gemini 2.5 Flash
playwright>=1.45,<2        # RPA de la Intranet Webix (op_cucs.fwx)
gspread>=6.0,<7            # Google Sheets (Service Account)
google-auth>=2.30          # Credenciales de la Service Account
